# ADC GPT HAL driver task (trigger)

All about configuration and working with trigger mode is inside main  
For you tasks:
1. Configure three channels to measure
2. Configure trigger on another source
